export default function NoData({ message }) {
  return (
    <div className="flex items-center justify-center py-12">
      <p className="w-full text-center text-xl font-semibold">{message}</p>
    </div>
  );
}
